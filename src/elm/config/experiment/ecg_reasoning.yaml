defaults:
  - ./sft_stage2.yaml

development: false
wandb: false

model:
  checkpoint: /p01/whan/refactor/ECG-Language-Models/src/runs/sft_stage2/1/checkpoints/epoch_best

gpu:
  strategy: null # null for one device, fsdp2 for multiple devices
  gradient_checkpointing: false