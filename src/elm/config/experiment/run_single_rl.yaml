defaults:
  - ../common.yaml

training:
  training_stage: rl
  batch_size: 2
  num_workers: 0

model:
  language_model: Qwen/Qwen3.5-0.8B-Base

data:
  data_names: # yaml list
    - ELM-Research/rl-stage1-noaug
  
  split_names: # corresponding to data_names
    - fold1_train
