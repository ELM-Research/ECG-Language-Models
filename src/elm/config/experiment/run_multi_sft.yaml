defaults:
  - ../common.yaml

training:
  optimizer: muon
  training_stage: sft
  batch_size: 2
  num_workers: 2

gpu:
  strategy: ddp

data:
  data_names: # yaml list
    - ELM-Research/sft-stage1-noaug
  split_names: # corresponding to data_names
    - fold1_train
