defaults:
  - ../qwen3.5-4b-muon.yaml

training:
  training_stage: sft
  batch_size: 16
  gradient_accumulation_steps: 2
  epochs: 3

optimizer:
  learning_rate: 5.0e-5

model:
  checkpoint: src/runs/sft_stage1/0/checkpoints/epoch_best
  trainable: [projector, language_model]
