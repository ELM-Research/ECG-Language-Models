defaults:
  - ./rl.yaml

development: true
wandb: false

model:
  checkpoint: src/runs/pretrain_stage1/0/checkpoints/epoch_best

gpu:
  strategy: null # null for one device, fsdp2 for multiple devices

evaluation:
  split_names: ["fold{fold}_test"]
  max_new_tokens: 1024

data:
  data_names: [ELM-Research/rl-ecg-r1]
  split_names: [fold1_test]