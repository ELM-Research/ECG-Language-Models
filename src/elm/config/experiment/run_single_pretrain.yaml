defaults:
  - ../common.yaml

training:
  training_stage: pretrain
  batch_size: 2
  num_workers: 0

model:
  language_model: Qwen/Qwen3.5-0.8B

data:
  data_names:
    - ELM-Research/pretrain-stage1
  split_names:
    - fold1_train
