defaults:
  - ../common.yaml

training:
  optimizer_name: muon
  training_stage: pretrain
  batch_size: 2
  num_workers: 0

model:
  language_model: Qwen/Qwen3.5-0.8B

data:
  data_names: # yaml list
    - ELM-Research/pretrain-stage1
  
  split_names: # corresponding to data_names
    - fold1_train
