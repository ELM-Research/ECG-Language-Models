defaults:
  - ../common.yaml

training:
  training_stage: sft
  batch_size: 12
  gradient_accumulation_steps: 1
  epochs: 3

optimizer:
  learning_rate: 5.0e-5

model:
  checkpoint: null
  trainable: [projector, language_model]

data:
  data_names: [ELM-Research/sft-stage2-noaug-filtered]
  split_names: [fold1_train]
